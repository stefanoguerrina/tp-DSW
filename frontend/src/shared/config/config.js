// Configuración central de la aplicación.
// Exporta constantes compartidas que usan múltiples features.

// URL base de la API del backend, definida en el archivo .env del frontend.
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL;
